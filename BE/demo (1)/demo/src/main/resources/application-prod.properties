# Production Configuration
spring.application.name=comic-store

# MongoDB Atlas (same as development)
spring.data.mongodb.uri=mongodb+srv://truc:Truc123@webbantruyen.2amdk17.mongodb.net/?retryWrites=true&w=majority&appName=webbantruyen
spring.data.mongodb.database=${MONGO_DB:comic_store}

# Server
server.port=8080

# JWT (use environment variables in production)
jwt.secret=${JWT_SECRET:your-secret-key-here-make-it-long-and-secure}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS - Update with your Render frontend URL
spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# Swagger UI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.tags-sorter=alpha
